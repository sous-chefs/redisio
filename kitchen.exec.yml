---
driver: { name: exec }
transport: { name: exec }
provisioner:
  name: chef_infra
  chef_omnibus_install: false
  chef_license: accept-no-persist
verifier:
  name: inspec
  sudo: true

platforms:
  - name: ubuntu-latest
