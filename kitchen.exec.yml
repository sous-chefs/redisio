---
driver: { name: exec }
transport: { name: exec }
provisioner:
  name: chef_infra
  policyfile_path: Policyfile.rb
  chef_omnibus_install: false
  chef_license: accept-no-persist
verifier:
  name: inspec

platforms:
  - name: ubuntu-latest
